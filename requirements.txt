# Core Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
gunicorn>=21.2.0
pydantic-settings>=2.0.0
pydantic[email]>=2.0.0
pyjwt>=2.8.0
slowapi>=0.1.9
logfire>=0.20.0
httpx>=0.25.0

# Database
psycopg2-binary>=2.9.9
redis>=5.0.0

# AI & Embeddings
google-generativeai>=0.3.0
groq>=0.4.0  # Ultra-fast LLM inference
# OpenAI/Claude/DeepSeek providers use plain HTTP (requests, already listed
# below) instead of their SDKs — no extra package/rebuild needed to enable them.

# Crawling & RSS
feedparser>=6.0.10
beautifulsoup4>=4.12.0  # HTML parsing for web scraping
requests>=2.31.0  # HTTP requests

# Utilities
python-dotenv>=1.0.0
APScheduler>=3.10.4  # Auto-scheduling for crawling
python-dateutil>=2.8.2  # Date parsing

# Security & Encryption
cryptography>=41.0.0  # Fernet encryption for sensitive data
python-multipart>=0.0.6  # File upload support
